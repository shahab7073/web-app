window.CAKE.pages['pages-index'] = function($, window, document) {

	/**
	 * Run the script
	 *
	 * @return void
	 */
	function run() {
		
	}

	return { run: run };

} (window.jQuery, window, document);